import { extendTheme } from "@chakra-ui/react";

// Customize Chakra UI theme variables here
// https://chakra-ui.com/docs/theming/customize-theme
const theme = {
  colors: {
    primary: "#C53030",
  },
};

export default extendTheme(theme);
